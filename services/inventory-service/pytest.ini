[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    postgres: requires a live Postgres instance (real row locking; skipped otherwise).
filterwarnings =
    ignore::DeprecationWarning
