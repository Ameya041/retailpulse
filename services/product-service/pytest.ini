[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    # SQLite has no native NUMERIC; the tests only assert on exact 2dp values,
    # and Postgres (the real target) stores NUMERIC precisely.
    ignore:.*support Decimal objects natively.*:Warning
