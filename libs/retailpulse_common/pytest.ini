[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    kafka: requires a live Kafka broker (skipped otherwise).
    redis: requires a live Redis instance (skipped otherwise).
filterwarnings =
    ignore::DeprecationWarning
