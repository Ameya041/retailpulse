# GENERATED FILE -- do not edit.
# Regenerate with: .\scripts\generate-k8s.ps1
# Source template: k8s/templates/worker.yaml.template
# Template for one Kafka consumer worker. Placeholders: inventory-worker, inventory-service.
#
# Deliberately a separate Deployment from the service that shares its image.
# The API scales on request latency; a consumer scales on partition count and
# consumer lag. Sharing a Deployment would mean scaling the web tier to clear
# a backlog, and every API replica joining the consumer group.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-worker
  namespace: retailpulse
  labels:
    app: inventory-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: retailpulse
spec:
  # One replica. Kafka assigns each partition to exactly one consumer in a
  # group, so replicas beyond the partition count sit idle -- scaling a
  # consumer past its partitions buys nothing.
  replicas: 1
  selector:
    matchLabels:
      app: inventory-worker
  strategy:
    # Recreate, not RollingUpdate. A rolling update briefly runs old and new
    # pods together, which triggers a consumer group rebalance and pauses
    # processing anyway. Stopping first makes the handover explicit and short.
    type: Recreate
  template:
    metadata:
      labels:
        app: inventory-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: retailpulse
    spec:
      # Long enough for the consumer to finish the message it is holding and
      # commit its offset. Cutting it short means that message is redelivered
      # on restart -- safe, because consumers are idempotent, but wasteful.
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: inventory-worker
          image: retailpulse/inventory-service:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.worker"]
          envFrom:
            - configMapRef: { name: retailpulse-config }
            - secretRef: { name: retailpulse-secrets }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          # No HTTP probes: a consumer serves no HTTP. A liveness probe here
          # would need a side-channel, and the signal that actually matters --
          # consumer lag -- is already in Prometheus.
          resources:
            requests: { cpu: 100m, memory: 192Mi }
            limits: { cpu: 500m, memory: 512Mi }
      volumes:
        - name: tmp
          emptyDir: {}

# Template for one Kafka consumer worker. Placeholders: order-worker, order-service.
#
# Deliberately a separate Deployment from the service that shares its image.
# The API scales on request latency; a consumer scales on partition count and
# consumer lag. Sharing a Deployment would mean scaling the web tier to clear
# a backlog, and every API replica joining the consumer group.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-worker
  namespace: retailpulse
  labels:
    app: order-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: retailpulse
spec:
  # One replica. Kafka assigns each partition to exactly one consumer in a
  # group, so replicas beyond the partition count sit idle -- scaling a
  # consumer past its partitions buys nothing.
  replicas: 1
  selector:
    matchLabels:
      app: order-worker
  strategy:
    # Recreate, not RollingUpdate. A rolling update briefly runs old and new
    # pods together, which triggers a consumer group rebalance and pauses
    # processing anyway. Stopping first makes the handover explicit and short.
    type: Recreate
  template:
    metadata:
      labels:
        app: order-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: retailpulse
    spec:
      # Long enough for the consumer to finish the message it is holding and
      # commit its offset. Cutting it short means that message is redelivered
      # on restart -- safe, because consumers are idempotent, but wasteful.
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: order-worker
          image: retailpulse/order-service:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.worker"]
          envFrom:
            - configMapRef: { name: retailpulse-config }
            - secretRef: { name: retailpulse-secrets }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          # No HTTP probes: a consumer serves no HTTP. A liveness probe here
          # would need a side-channel, and the signal that actually matters --
          # consumer lag -- is already in Prometheus.
          resources:
            requests: { cpu: 100m, memory: 192Mi }
            limits: { cpu: 500m, memory: 512Mi }
      volumes:
        - name: tmp
          emptyDir: {}

# Template for one Kafka consumer worker. Placeholders: payment-worker, payment-service.
#
# Deliberately a separate Deployment from the service that shares its image.
# The API scales on request latency; a consumer scales on partition count and
# consumer lag. Sharing a Deployment would mean scaling the web tier to clear
# a backlog, and every API replica joining the consumer group.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-worker
  namespace: retailpulse
  labels:
    app: payment-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: retailpulse
spec:
  # One replica. Kafka assigns each partition to exactly one consumer in a
  # group, so replicas beyond the partition count sit idle -- scaling a
  # consumer past its partitions buys nothing.
  replicas: 1
  selector:
    matchLabels:
      app: payment-worker
  strategy:
    # Recreate, not RollingUpdate. A rolling update briefly runs old and new
    # pods together, which triggers a consumer group rebalance and pauses
    # processing anyway. Stopping first makes the handover explicit and short.
    type: Recreate
  template:
    metadata:
      labels:
        app: payment-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: retailpulse
    spec:
      # Long enough for the consumer to finish the message it is holding and
      # commit its offset. Cutting it short means that message is redelivered
      # on restart -- safe, because consumers are idempotent, but wasteful.
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: payment-worker
          image: retailpulse/payment-service:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.worker"]
          envFrom:
            - configMapRef: { name: retailpulse-config }
            - secretRef: { name: retailpulse-secrets }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          # No HTTP probes: a consumer serves no HTTP. A liveness probe here
          # would need a side-channel, and the signal that actually matters --
          # consumer lag -- is already in Prometheus.
          resources:
            requests: { cpu: 100m, memory: 192Mi }
            limits: { cpu: 500m, memory: 512Mi }
      volumes:
        - name: tmp
          emptyDir: {}

# Template for one Kafka consumer worker. Placeholders: fulfilment-worker, fulfilment-service.
#
# Deliberately a separate Deployment from the service that shares its image.
# The API scales on request latency; a consumer scales on partition count and
# consumer lag. Sharing a Deployment would mean scaling the web tier to clear
# a backlog, and every API replica joining the consumer group.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fulfilment-worker
  namespace: retailpulse
  labels:
    app: fulfilment-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: retailpulse
spec:
  # One replica. Kafka assigns each partition to exactly one consumer in a
  # group, so replicas beyond the partition count sit idle -- scaling a
  # consumer past its partitions buys nothing.
  replicas: 1
  selector:
    matchLabels:
      app: fulfilment-worker
  strategy:
    # Recreate, not RollingUpdate. A rolling update briefly runs old and new
    # pods together, which triggers a consumer group rebalance and pauses
    # processing anyway. Stopping first makes the handover explicit and short.
    type: Recreate
  template:
    metadata:
      labels:
        app: fulfilment-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: retailpulse
    spec:
      # Long enough for the consumer to finish the message it is holding and
      # commit its offset. Cutting it short means that message is redelivered
      # on restart -- safe, because consumers are idempotent, but wasteful.
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: fulfilment-worker
          image: retailpulse/fulfilment-service:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.worker"]
          envFrom:
            - configMapRef: { name: retailpulse-config }
            - secretRef: { name: retailpulse-secrets }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          # No HTTP probes: a consumer serves no HTTP. A liveness probe here
          # would need a side-channel, and the signal that actually matters --
          # consumer lag -- is already in Prometheus.
          resources:
            requests: { cpu: 100m, memory: 192Mi }
            limits: { cpu: 500m, memory: 512Mi }
      volumes:
        - name: tmp
          emptyDir: {}

# Template for one Kafka consumer worker. Placeholders: analytics-worker, analytics-service.
#
# Deliberately a separate Deployment from the service that shares its image.
# The API scales on request latency; a consumer scales on partition count and
# consumer lag. Sharing a Deployment would mean scaling the web tier to clear
# a backlog, and every API replica joining the consumer group.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-worker
  namespace: retailpulse
  labels:
    app: analytics-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: retailpulse
spec:
  # One replica. Kafka assigns each partition to exactly one consumer in a
  # group, so replicas beyond the partition count sit idle -- scaling a
  # consumer past its partitions buys nothing.
  replicas: 1
  selector:
    matchLabels:
      app: analytics-worker
  strategy:
    # Recreate, not RollingUpdate. A rolling update briefly runs old and new
    # pods together, which triggers a consumer group rebalance and pauses
    # processing anyway. Stopping first makes the handover explicit and short.
    type: Recreate
  template:
    metadata:
      labels:
        app: analytics-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: retailpulse
    spec:
      # Long enough for the consumer to finish the message it is holding and
      # commit its offset. Cutting it short means that message is redelivered
      # on restart -- safe, because consumers are idempotent, but wasteful.
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: analytics-worker
          image: retailpulse/analytics-service:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.worker"]
          envFrom:
            - configMapRef: { name: retailpulse-config }
            - secretRef: { name: retailpulse-secrets }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          # No HTTP probes: a consumer serves no HTTP. A liveness probe here
          # would need a side-channel, and the signal that actually matters --
          # consumer lag -- is already in Prometheus.
          resources:
            requests: { cpu: 100m, memory: 192Mi }
            limits: { cpu: 500m, memory: 512Mi }
      volumes:
        - name: tmp
          emptyDir: {}

