apiVersion: 1

providers:
  - name: RetailPulse
    orgId: 1
    folder: RetailPulse
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
